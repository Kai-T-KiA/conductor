// import AdminLayout from '@/components/layouts/AdminLayout';
import TaskManagement from '../../components/TaskManagement'; // 先ほど作成したコンポーネントのパス

export default function AdminTasksPage() {
  return (
    <main>
      <TaskManagement />
    </main>
  );
}